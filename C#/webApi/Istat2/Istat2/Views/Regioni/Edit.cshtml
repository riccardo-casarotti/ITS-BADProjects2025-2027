@model Istat2.Models.Regione

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Regione</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Denominazione" class="control-label"></label>
                <input asp-for="Denominazione" class="form-control" />
                <span asp-validation-for="Denominazione" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdRipartizione" class="control-label"></label>
                <select asp-for="IdRipartizione" class="form-control" asp-items="ViewBag.IdRipartizione"></select>
                <span asp-validation-for="IdRipartizione" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
