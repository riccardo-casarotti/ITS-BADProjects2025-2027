@model IEnumerable<BikeStoresProject.Models.Store>

@{
    ViewData["Title"] = "Stores";
}

<h1>@ViewData["Title"]</h1>

<form asp-action="Index" method="get">
    <p>
        Cerca: <input type="text" name="searchTesto" value="@ViewBag.SearchTesto" class="form-control" style="display:inline; width:300px;" placeholder="Cerca Città o Stato" />
        <input type="submit" value="Cerca" class="btn btn-primary" />
        <a asp-action="Index" class="btn btn-outline-secondary">Rimuovi filtro</a>
    </p>
</form>

<table class="table table-striped table-hover align-middle shadow-sm">
    <thead class="table-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StoreName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.City)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.State)
            </th>
            <th class="text-center">Azioni</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StoreName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td>
                <span class="badge bg-secondary">@Html.DisplayFor(modelItem => item.State)</span>
            </td>
            <td class="text-center">
                <a asp-action="Details" asp-route-id="@item.StoreId" class="btn btn-sm btn-outline-primary">Details</a>
            </td>
        </tr>
}
    </tbody>
</table>